import * as apiKeySchema from './api-key-schema.json';
import * as swapQuoteRequestSchema from './swap-quote-request-schema.json';

export const schemas = {
    apiKeySchema,
    swapQuoteRequestSchema,
};
